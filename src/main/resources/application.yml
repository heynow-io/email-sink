heynow.sink.email:
  provider: SYSTEM_OUT
  system-out.sender: dev@heynow.io

spring:
  application:
    name: email-sink!${heynow.sink.email.provider}
  cloud:
    stream:
      bindings:
        input:
          destination: alertInput
